#include <iostream>

using namespace std;


vector <int> ordre (n);
vector <int> ordre2 (n);
for(int e=0;e<n;e++)
{
	for (int i=0; i<n; i++){
		ordre[i]=i;


		for (int j=i+1;j<i-1; j++) {
			ordre2[i]=ordre[j];
			ordre2[n]=ordre[1]
		}
	}

lgc(const vector <int> ordre (n), double &lg1,const int n, const int P[][2]);

		lgc(const vector <int> ordre2 (n), double &lg1,const int n, const int P[][2]);
              


if (lgc(const vector <int> ordre2 (n), double &lg1,const int n, const int P[][2])<lgc(const vector <int> ordre (n), double &lg1,const int n, const int P[][2]))
{
	vector <int> ordre2 (n) =vector <int> ordre (n);
}

}
cout<<"le chemin le plus court est : "<< vector <int> ordre (n) <<endl;
